<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="creationsofali.asynctaskdemo.MainActivity">

    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/activity_horizontal_margin"
        android:gravity="center"
        android:padding="8dp"
        android:text="Please provide sleep time below"
        android:textColor="#555"
        android:textSize="18sp" />

    <EditText
        android:id="@+id/etSleepTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvInstruction"
        android:background="@drawable/shape_edit_text"
        android:gravity="center_horizontal"
        android:hint="sleep time (seconds)"
        android:imeOptions="actionDone"
        android:inputType="number"
        android:padding="12dp"
        android:textColorHint="@color/colorHint" />

    <Button
        android:id="@+id/buttonRunAsync"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etSleepTime"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="120dp"
        android:background="@color/colorAccent"
        android:text="Run Async Task"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/tvAfterTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonRunAsync"
        android:layout_centerHorizontal="true"
        android:layout_margin="@dimen/activity_horizontal_margin"
        android:padding="10dp"
        android:textColor="#555"
        android:textSize="18sp" />


</RelativeLayout>
